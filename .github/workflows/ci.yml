name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-typescript:
    name: Lint & Format (TypeScript)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared (for type-aware lint)
        run: pnpm --filter @open-inspect/shared build

      - name: Format check
        run: pnpm fmt:check

      - name: Lint
        run: pnpm lint

  typecheck-typescript:
    name: TypeCheck (TypeScript)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @open-inspect/shared build

      - name: TypeCheck all packages
        run: pnpm run typecheck

  build-web:
    name: Build (Web)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @open-inspect/shared build

      - name: Build web package
        run: pnpm --filter @open-inspect/web build

  lint-python-sandbox-runtime:
    name: Lint & Format (Python - sandbox-runtime)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: packages/sandbox-runtime
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run Ruff linter
        run: ruff check src/ tests/

      - name: Run Ruff formatter check
        run: ruff format --check src/ tests/

  lint-python:
    name: Lint & Format (Python - modal-infra)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: packages/modal-infra
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ../sandbox-runtime
          pip install -e ".[dev]"

      - name: Run Ruff linter
        run: ruff check src/

      - name: Run Ruff formatter check
        run: ruff format --check src/

  typecheck-python:
    name: TypeCheck (Python)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: packages/modal-infra
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ../sandbox-runtime
          pip install -e ".[dev]"

      - name: Run MyPy
        run: mypy src/
        continue-on-error: true # Allow failures initially as types are added

  typecheck-python-sandbox-runtime:
    name: TypeCheck (Python - sandbox-runtime)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-python-sandbox-runtime]
    defaults:
      run:
        working-directory: packages/sandbox-runtime
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run MyPy
        run: mypy src/
        continue-on-error: true # Allow failures initially as types are added

  test-cp-unit:
    name: Test (control-plane unit)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-typescript, typecheck-typescript]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @open-inspect/shared build

      - name: Run shared tests
        run: pnpm --filter @open-inspect/shared test

      - name: Run control-plane unit tests
        run: pnpm --filter @open-inspect/control-plane test

  test-cp-integration:
    name: Test (control-plane integration)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-typescript, typecheck-typescript]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @open-inspect/shared build

      - name: Install workerd runtime dependency
        run: sudo apt-get update && sudo apt-get install -y libc++1

      - name: Run control-plane integration tests
        run: pnpm --filter @open-inspect/control-plane run test:integration

  test-web:
    name: Test (web)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-typescript, typecheck-typescript]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @open-inspect/shared build

      - name: Run web tests
        run: pnpm --filter @open-inspect/web test

  test-bots:
    name: Test (bots)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-typescript, typecheck-typescript]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @open-inspect/shared build

      - name: Run github-bot tests
        run: pnpm --filter @open-inspect/github-bot test

      - name: Run slack-bot tests
        run: pnpm --filter @open-inspect/slack-bot test

      - name: Run linear-bot tests
        run: pnpm --filter @open-inspect/linear-bot test

  test-python-sandbox-runtime:
    name: Test (Python - sandbox-runtime)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-python-sandbox-runtime]
    defaults:
      run:
        working-directory: packages/sandbox-runtime
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest tests/ -v

  test-python:
    name: Test (Python - modal-infra)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint-python]
    defaults:
      run:
        working-directory: packages/modal-infra
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ../sandbox-runtime
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest tests/ -v
